--- build_gcc.sh	2026-03-24 14:13:54.365166970 +0000
+++ build_gcc_intel.sh	2026-03-24 14:29:56.940571224 +0000
@@ -77,7 +77,7 @@
 if [ -n "$APPLE_GCC" ]; then
   # work around broken multilib support
   ARCHS_TO_BUILD+=" $GCC_ARCH32"
-  EXTRA_CONFFLAGS+="--with-gxx-include-dir=/usr/include/c++/4.0.0 " ## TODO: version
+  EXTRA_CONFFLAGS+="--with-gxx-include-dir=/usr/include/c++/4.2.1 " ## TODO: version
 
   # fix -lstdc++ link
   if [ ! -f "$OSXCROSS_SDK/usr/lib/libstdc++.dylib" ]; then
@@ -121,8 +121,8 @@
 
 if [ -n "$APPLE_GCC" ]; then
   pushd gcc*$APPLE_GCC_VERSION &>/dev/null
-  [ -z "$CC" ] && CC=cc
-  export CC+=" -std=gnu89"
+  [ -z "$CC" ] && CC=gcc
+  export CC+=" -std=gnu89 -fgnu89-inline -Wno-narrowing"
 else
   pushd gcc*$GCC_VERSION* &>/dev/null
 fi
