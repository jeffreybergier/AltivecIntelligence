#include <stdio.h>

int __isPlatformVersionAtLeast(int p, int maj, int min, int rev) {
    printf("[STUB] isPlatformVersionAtLeast called: p=%d, maj=%d, min=%d, rev=%d\n", p, maj, min, rev);
    return 1;
}
