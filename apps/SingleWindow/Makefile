# Altivec Intelligence - SingleWindow (Mac)
APP_NAME = SingleWindow
RES_DIR = Resources
INFO_PLIST = Info.plist
SOURCES = main.m AppDelegate.m

# Import common Mac build engine
include ../../altivec_common_mac.mk
